
//write a function with hours in input and the number of minutes to midnight in output

function minutesInDay(hours, minutes) {
const result = minutesInDay(23, 59);
console.log(result, result === 1);
}